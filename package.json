{
  "name": "custom-activity-blip",
  "version": "1.0.0",
  "description": "SFMC Custom Activity — WhatsApp via BLIP (Heroku dev)",
  "main": "server.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "dependencies": {
    "body-parser":   "^1.20.2",
    "express":       "^4.18.2",
    "jsonwebtoken":  "^9.0.2"
  }
}
